package wp_midterm_2019253025;

public class Medic extends Unit{
	public Medic() {	// Medic의 생성자
		super("Medic", 60, 0, 1, 1);	// name으로 Medic, maxhp값으로 60, atk값으로 0, armor값으로 1, unitPopulation값으로 1을 저장(Unit class의 생성자를 이용)
	}
}