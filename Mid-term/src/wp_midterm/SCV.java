package wp_midterm_2019253025;

public class SCV extends Unit{
	
	public SCV() {	// SCV의 생성자
		super("SCV", 60, 5, 0, 1);	// name으로 SCV, maxhp값으로 60, atk값으로 5, armor값으로 0, unitPopulation값으로 1을 저장(Unit class의 생성자를 이용)
	}

}